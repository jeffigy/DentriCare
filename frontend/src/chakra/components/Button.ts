import { ComponentStyleConfig } from "@chakra-ui/react";

export const ButtonTheme: ComponentStyleConfig = {
  baseStyle: {
    borderRadius: "md",
  },
  defaultProps: {
    colorScheme: "primary",
  },
};
